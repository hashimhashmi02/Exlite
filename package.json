{
  "name": "exness-clone",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": ">=20"
  },
   "scripts": {
    "bootstrap": "npm --prefix api i && npm --prefix web i",
    "dev:api": "npm --prefix api run dev",
    "dev:web": "npm --prefix web run dev"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19"
  }
}
